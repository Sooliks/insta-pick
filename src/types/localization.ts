export type LocalizationType = {
    metadata: {
        title: string
        mainDescription: string
    }
    components: {
        selectLanguageLabel: string
        mainMenu: {
            exit: string
        }
        buttons: {
            start: string
        }
    }
}